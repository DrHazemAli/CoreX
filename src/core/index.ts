/**
 * ============================================================================
 * COREX: Core Domain Layer Exports
 * Description: Public API for pure business logic
 *
 * The core layer contains only pure functions with no external dependencies.
 * It can be used anywhere: server, client, workers, edge functions.
 *
 * @example
 * ```ts
 * import { myDomainFunction } from '@/core';
 * ```
 * ============================================================================
 */

// Core types
export * from "./types";

// Add your domain logic exports here
// export { myFunction } from './my-domain';
